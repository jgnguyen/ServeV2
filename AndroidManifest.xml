<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.cs.fsu"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="10" />
	<uses-permission android:name="android.permission.INTERNET" />
    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" 
        android:theme="@android:style/Theme.Black.NoTitleBar">

        
        <activity
            android:name=".main"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".createAccount"
		          android:label="@string/createAccountName">
		</activity>
		<activity android:name=".signIn"
		          android:label="@string/signInName">
		</activity>
		<activity android:name=".sessionPicker"
		          android:label="@string/sessionPickerName">
		</activity>
		<activity android:name=".sessionNew"
		          android:label="@string/sessionNewName">
		</activity>
		<activity android:name=".sessionExisting"
		          android:label="@string/sessionExistingName">
		</activity>
		<activity android:name=".sessionResults"
		          android:label="@string/sessionResultsName">
		</activity>
		<activity android:name=".sessionForm"
		          android:label="@string/sessionFormName">
		</activity>
    </application>

</manifest>